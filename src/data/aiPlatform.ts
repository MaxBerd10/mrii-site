/** Live AiShifokor platform — clinical AI for physicians, radiology, ultrasound, and more. */
export const AISHIFOKOR_URL = 'https://aishifokor.uz/'

/** Same-tab open so the browser Back button returns to the clinic site. */
export function openAiPlatform() {
  window.location.assign(AISHIFOKOR_URL)
}
