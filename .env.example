# Frontend → Django CMS API (no trailing slash)
# Local Docker Compose API:
VITE_API_URL=http://127.0.0.1:8001

# Production example:
# VITE_API_URL=https://api.example.com

# Leave empty to use static fallback content (no CMS).
