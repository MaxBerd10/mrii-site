---
version: 1
slug: "src-pages-doctorpage-tsx"
primary_target: "src/pages/DoctorPage.tsx"
related_targets: []
---

Information-rich professional doctor dossier in Operate mode. The route is explicitly light-only. One committed cobalt identity field owns 31% of the first surface; the wider daylight record owns 69%. It uses adaptive viewport gutters rather than a restrictive max-width.

The first viewport proves professional depth rather than leading with a generic portrait hero. The left credential field holds a moderate portrait, identity, experience, honest request availability, one booking action, and phone. The right record turns the full career into one chronological clinical trace with years, workplaces, and roles. Education, consultation languages with levels, care focus, papers, and studies sit in open ruled records instead of a card grid.

Exact workplace history is not yet present in the current data model. `doctorDossier.ts` provides a visibly disclosed deterministic demo fallback derived from total experience; existing education, languages, research counts, and care focus remain source-driven. A future CMS `career[]` replaces only that fallback.

The global nav suppresses its booking action on doctor-detail routes so the surface has one primary CTA. Reviews keep progressive disclosure, focus return, and live success feedback. Related doctors use varied real portraits.

Motion is one 8px, 240ms first-load settle plus direct press/state feedback. There are no scroll reveals or bounces. Fine-pointer hover is gated; reduced motion removes position changes. Below 900px the identity field becomes a compact two-column credential header; below 720px the career trace becomes a vertical chronology and the surface becomes linear without horizontal page scroll.
